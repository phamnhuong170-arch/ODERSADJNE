{
  "name": "odersad-note",
  "version": "4.0.0",
  "description": "App Note từ HTML",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp ODERSADNote.html www/index.html",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
