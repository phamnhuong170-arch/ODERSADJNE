{
  "name": "odersad-note-v4",
  "version": "4.0.0",
  "private": true,
  "scripts": {
    "prepare-assets": "mkdir -p www && cp ODERSADNote.html www/index.html",
    "build-android": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
